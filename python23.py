# Function คือ การทำงานหนึ่งๆ
# คือวิธีการที่ใช้เพื่อสร้างและใช้งานฟังก์ชัน
# จะไม่ทำงาน หากไม่เรียกใช้ (call function)
# Function 4 ประเภท
# มี parameter มี return
# มี parameter ไม่มี return
# ไม่มี parameter มี return
# ไม่มี parameter ไม่มี retrun

# user-defind function (user = dev.)
